# Requirements for System Health Monitor
# Install with: pip install -r requirements.txt
#
# Notes:
# - Tested logic against psutil 7.x (confirmed installed/working in dev).
# - fastapi/uvicorn/sqlalchemy versions below are current-stable as of
#   mid-2026; exact pins could not be verified by actually installing them
#   in the environment these files were generated in (no network access),
#   so double-check `pip list` after install matches what's below.
# - Python 3.10+ required (FastAPI 0.13x requires Python >=3.10).

# --- config.py ---
psutil>=7.0.0

# --- database.py ---
SQLAlchemy>=2.0.0,<3.0.0

# --- api.py ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.0.0,<3.0.0